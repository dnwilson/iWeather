<%= turbo_stream.replace :forecast, partial: "forecast",
                                    locals: { extended_forecast: @extended_forecast,
                                              today: @extended_forecast.forecasts.first } %>